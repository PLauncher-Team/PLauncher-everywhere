[project]
name = "plauncher-everywhere"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "ctkscrollabledropdownpp",
    "customtkinter",
    "filelock",
    "minecraft-launcher-lib",
    "pillow",
    "psutil",
]
