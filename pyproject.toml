[project]
name = "plauncher-everywhere"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ctkscrollabledropdownpp",
    "customtkinter",
    "minecraft-launcher-lib",
    "pillow",
]
